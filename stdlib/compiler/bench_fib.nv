// bench_fib.nv — carga pesada: fib(26) + suma de fibonacci + loop de 100k
funcion entero fib(entero n) {
    si (n <= 1) { retornar n; }
    retornar fib(n - 1) + fib(n - 2);
}

entero total = 0;
entero i = 0;
mientras i < 100000 {
    total = total + i;
    i = i + 1;
}
imprimir(fib(26), total);
