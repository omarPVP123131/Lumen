importar ingles;

string s = "Hola Mundo Lumen";
string invertido = "";
integer i = __str_longitud(s) - 1;
while (i >= 0) {
    invertido = invertido + s[i];
    i = i - 1;
}
print(invertido);

string s2 = "abcdef";
string mayus = __str_mayusculas(s2);
string minus = __str_minusculas(s2);
print(mayus);
print(minus);

if __str_contiene(s, "Mundo") {
    print("contiene Mundo");
}
