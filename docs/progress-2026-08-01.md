Progreso — 2026-08-01 — LÚMEN v3.0.0 Producción Real (actualizado 21 Ago 2026)

> **v3.0.0 Producción Real (21 Ago 2026):** fixes escalables `last_significant()` + `label_counter` global, `CHUNK_VERSION 7` con `FuncMeta.defaults` + `bind_args` unificado, `stdlib/graficos.nv:es_headless()` (`LUMEN_HEADLESS`/`CI`), bench 8 (`cargo bench -p lumen-bench`), 616 e2e + 9 production = 673 vm tests (917 workspace), CI `headless-check` con `LUMEN_HEADLESS=1 CI=1`. Ver [docs/produccion.md](produccion.md). `VERSION` 3.0.0.

Progreso — 2026-08-01 (histórico)

Resumen breve:
- Diagnóstico y corrección en lexer/self-hosted compiler_v4.nv para resolver tokens truncados.
- Reemplazada extracción de subcadenas por sub_from_chars sobre listas de caracteres para evitar corrupciones al normalizar CRLF.
- Añadida normalización consistente (s2) y helper sub_from_chars a compiler_v4.nv; lexer.nv usa sub_from_chars.
- Instrumentación: ORIG_FULL / NORM_FULL y dumps detallados para mismatches.

Siguientes pasos:
1. Ejecutar el pipeline self-hosted y validar que `retornar`, `items`, `numero` tokenizan correctamente.
2. Revertir heurísticas temporales cuando el lexer esté estable.
3. Añadir tests de tokenización CR/LF/LF-only.

Archivos modificados:
- stdlib/compiler/lexer.nv
- stdlib/compiler/compiler_v4.nv

Co-autores: Copilot

---

## Sesión tarde-noche (18:00-18:40, AI + usuario) — Sprint 6.4

### Hechos
- **MISTERIO ORIG_FULL resuelto**: el .nvc de 5:33 PM (75,643 B, con ORIG_FULL) vino de un compiler_v4.nv del usuario que YA NO existe; los fuentes actuales (lexer.nv 5:15 PM) llaman `__str_reemplazar` como builtin. Nunca hubo inconsistencia real: faltaba el builtin en Rust.
- **Builtins nativos añadidos** (cargo build --release ✓, 3x 11-16 s):
  - `__str_reemplazar`/`__str_replace` = s.replace — vm.rs ~345 y ~3088, sema.rs ~2430 (→ Texto), **builder.rs ~992 (matches! de calls — CRÍTICO: sin esto IR emite Load + CallValue @0 → Variable no definida)**.
  - `__str_subcadena_chars`/`__str_slice_chars` = lista de chars → string (clamps).
- **lexer.nv (permanente)**: `sub_from_chars(cs, st, fin)` = one-liner al builtin; parámetro renombrado `en`→`fin` porque **`en` es keyword** (E011 [219:65]/E020 [220:44]).
- **Hang de test_stdlib_mini MUERTO** → CORRECTO (coleccion_contar: 2). La causa no era ast_to_text: era sub_from_chars O(n²) en VM interpretada + builtin inexistente.
- **BUG RAÍZ de stress_test encontrado y FIXEADO en parser.nv (~142)**: el scan de "genéricos en llamada foo<T>(...)" iba del `<` al primer `>` sin límites → en `mientras (i < largo(arr))` el menor-que disparaba el scan hasta el `>` de un `lista<entero>` posterior (tokens 91→184) → AST malformado (modulo/Punto/top-level comidos, JmpIf targets rotos, __main__ vacío).
  - Fix: abortar el scan en `(` `)` `{` `}` `;` y solo consumir si tras `>` viene `(`.
- **Estado post-fix**: test_arr ✅ · foreach ✅ · test_stdlib_mini ✅ · stress_test 🟡 (compila todo; único fallo `Variable 'Punto' no definida` = gap estructura) · demo_completo pendiente.
- **Higiene**: target.txt → stdlib/coleccion.nv/stdlib/compile.nvc · root compiler_v4.nv restaurado (86,062 B; el ps1 con CWD incorrecto lo había pisado con 1,918 B) · instrumentación revertida al regenerar (86,062 B, check ✓, .nvc 73,120 B).

### Siguientes pasos (orden sugerido)
1. `estructura` real en parser.nv/codegen.nv: StructInit → __map_nuevo/__map_poner; acceso .campo → __map_get (gap stress_test).
2. `enum`/`elegir`/`sea`.
3. Re-verificar demo_completo (bug T — probablemente arreglado por el fix de genéricos).
4. Fixpoint v4 → fuego.ps1 (29/115 → subir).
5. Docs finales.

### Lecciones
- **builder.rs matches!**: TODO builtin nuevo debe registrarse ahí o la llamada se emite como Load+CallValue.
- **generar_v4.ps1**: CWD obligatorio stdlib/compiler (rutas relativas).
- **mini_fuego.ps1**: ejemplos como parámetro; correr de a 1-2 (el loop corta si uno tarda).
- Instrumentación de debug: añadir SOLO al archivo generado (se pierde al regenerar); NO tocar parser.nv/lexer.nv fuentes con dumps temporales.

Archivos modificados (sesión): crates/lumen-vm/src/vm.rs, crates/lumen-sema/src/sema.rs, crates/lumen-ir/src/builder.rs, stdlib/compiler/lexer.nv, stdlib/compiler/parser.nv, stdlib/compiler/compiler_v4.nv (generado), stdlib/compiler/compiler_v4.nvc, AGENTS.md, docs/self-hosting.md.

---

## Sesion 20:00-21:20 — demo_completo CORRECTO + Fixpoint v4

### Logro principal
**demo_completo.nv: 89/89 lineas, 0 diffs** via el compilador autocontenido (compiler_v4.nv 94,789 B) + **stress_test 8/8, 0 diffs** + **FIXPOINT v4 byte-IDENTICO**: compiler_v4.nvc (Rust-built) -> compiler_v4_self.nvc (91,806 B) -> compiler_v4_self2.nvc (91,806 B, SequenceEqual=True).

### Bugs raiz encontrados (causa real de los sintomas viejos)
1. **Float 3.14159 -> 3**: el LEXER partia `3.14159` en `[3][.][14159]` (bucle de digitos no consumia `.`). Fix lexer.nv: `mientras i < n && (_lx_es_digit(chars[i]) || chars[i] == ".")`. Ademas vm.rs codegen_to_nvc: op 1 (PushNum) emitia PushInt (opcode 1); ahora emite PushNum (opcode 2) con f64 en tabla nums. **Ambos fixes son necesarios** (lexer sin dot -> PushInt sin el fix de vm.rs; con dot -> PushNum que vm.rs truncaba).
2. **`arr.agregar(4)` sin efecto**: el desugar parser.nv:279-289 crea nodo `Assign` DENTRO de ExprStmt; `_gen_expr` no tenia caso "Assign" (solo `_gen_stmt` ~354) -> la sentencia emitia 0 instrucciones (evidencia: output 3/3/1, sin "agregar" en names). Fix: caso Assign en `_gen_expr` (espejo de _gen_stmt, branch target -> OP_ARRAY_SET).
3. **Error "Ge requires numbers or strings" del demo = TERNARIO** (no era el if del ternario ni otra cosa):
   - Precedencia: el check `?` en `_parse_bin` (parser.nv:387) disparaba en cualquier nivel de recursion -> `edad >= 18 ? A : B` = `(edad >= (18 ? A : B))`. Fix: `si (mp == 0 && _st_ch(st, 4, "?"))`.
   - Orden de ramas: el handler Ternario (codegen.nv:325) asumia JmpIf=jump-if-TRUE pero el VM salta con FALSY (vm.rs:3688 `if !val.is_truthy()`). Fix: `si` al fallthrough + Jmp, `no` al salto.
   - Verificado con debug temporal en vm.rs (revertido): `Ge @ip=300 a=Int(20) b=Str("Menor")` — el Int 20 quedaba del izq del Binary exterior y el Str del branch interior.

### Notas de proceso
- **AST_DUMP removido** de generar_v4.ps1: `ast_a_texto` es O(n^2) (concat de strings) y cuelga el fixpoint en fuentes de ~95 KB (el demo de 4 KB tardaba segundos; el self-compile se colgaba >15 min).
- **Hang falso en consola**: `& exe 2>&1 | Select-String ... | Out-String` con pipe de PowerShell puede colgarse/bufferear; usar Start-Process + -RedirectStandardOutput para runs largos.
- **CWD**: el compiler_v4.nvc lee `stdlib/compiler/target.txt` relativo al CWD — correr desde la raiz del repo.
- **Fixpoint con el compilador nuevo**: ~10-15 min (sin AST_DUMP); demo ~15 s. Optimizacion pendiente (Sprint 7 vm.nv).

### Estado
- demo_completo 89/89, stress_test 8/8, mini_agregar (.agregar) 3/4/1, test_arr 5/1/5.
- cargo test: todos OK (~385 tests).
- root compiler_v4.nv copiado (94,789 B).
- Pendientes: `enum`/`elegir`/`sea` reales (parsing tolerante sin codegen) -> fuego.ps1 con imports prefijados -> Sprint 7 VM en LUMEN -> Sprint 8 dogfooding + release v2.4.6.

Archivos modificados (sesion): crates/lumen-vm/src/vm.rs, stdlib/compiler/lexer.nv, stdlib/compiler/parser.nv, stdlib/compiler/codegen.nv, stdlib/compiler/generar_v4.ps1, stdlib/compiler/compiler_v4.nv (generado 94,789 B), stdlib/compiler/compiler_v4.nvc (reconstruido), compiler_v4.nv (root), AGENTS.md.

## Sesion 21:30-23:30 - 6.4 enum/elegir/sea reales + BUG del mapa persistente
### Logro principal
**enum/elegir/sea REALES en el compilador autocontenido.** match.nv IDENTICAL (2 casos + defecto), enums.nv IDENTICAL (variantes unit + con datos + print directo), demo 90/90 0 diffs. **fuego.ps1: 389/389 compilan, 60 CORRECTOS** (vs 29 en Sprint 6 - se DOBLO), 54 incompatibles (gaps FFI/red/lib), 2 timeouts, **0 fallos**. Los ejemplos nuevos CORRECTO: enums, match, structs, demo_completo, stress_test, y 17 tui_test_min*.

### BUG RAIZ: __map_poner NO muta el mapa (im::HashMap persistente)
- `im::HashMap` (vm.rs:2) es ESTRUCTURALMENTE INMUTABLE: insert devuelve una COPIA nueva, el mapa original NO cambia.
- El patron `last = __map_poner(last, "sino", ifn)` del chain de `elegir` JAMAS propagaba a `first` -> solo se compilaba el caso 1.
- El demo 89/89 lo ENMASCARABA: solo llama `color_str(Color::Rojo)` = caso 1 (verificado en la fuente).
- **Fix**: `ifns[]` (lista por indice) + reconstruccion de cadena desde el FINAL: `chain = __map_poner(ifns[i2], "sino", chain)` descendiendo. La semantica clone-copy es ESENCIAL para el parser (probes `st_dos = _st_adv(st)` necesitan copias independientes).
- Esto explica por que los chains If de `sino si` funcionaban (no mutan nodos compartidos) pero `elegir` no.

### Features implementadas
- `sea`/`let` -> VarDecl real (parser.nv:456): `sea [tipo] nm = expr;` (tipo_var="let" o el tipo). Antes: skip-keyword.
- `Nombre::Miembro(args)` -> nodo `EnumInit` (parser.nv:187): args parenth optional (unit variants argc=0). El VM imprime `Resultado::Exitoso(100)` nativo (Display Value::Enum) y `Eq` compara name+variant+fields (vm.rs:2551-2562).
- `defecto:`/`default:` REAL: break-condition ahora incluye "defecto"; post-check consume la kw, parsea el body -> se adjunta como `sino` del ultimo If (o Block si solo-default).
- Codegen: OP_ENUM_CTOR=43/OP_ENUM_VAR=44/OP_ENUM_ARGC=45 + caso EnumInit (args en orden + trio consecutivo nombre/var/argc).
- vm.rs codegen_to_nvc: 43->WithIdx(EnumCtor, str); 44->WithIdx(Nop, str); 45->argc (ints->f64) en nums + WithIdx(Nop, num). El handler VM EnumCtor lee ip+1/ip+2.
- Limpieza: debug residual LEX-LOOP/LEX-DBG "retornar" eliminado de lexer.nv; AST dump temporal removido del main.

### Pendiente
- Revisar fixpoint v4 en curso (regresion critica tras elegir -> chain rebuild).
- Sprint 7: VM en LUMEN (vm.nv) + optimizacion (fixpoint ~10-18 min, demo ~15s).
- Sprint 8: dogfooding stdlib + docs + release v2.4.6.
- Gaps de fuego restantes visibles: `Variable 'ninguno'` (opcion None), `Variable 'resultado'`, tuplas `(a,b,c)`, FFI (red_/io_/texto_...), `largo()` con Int.

Archivos: stdlib/compiler/parser.nv (sea/::/defecto/elegir-chain), stdlib/compiler/codegen.nv (43/44/45 + EnumInit), crates/lumen-vm/src/vm.rs (codegen_to_nvc ops 43/44/45), stdlib/compiler/lexer.nv (limpieza), stdlib/compiler/compiler_v4.nv (regenerado 97,985 B), AGENTS.md.


## Sesión AI 2 Ago 20:00-21:30 — Sprint 6.5: cortocircuito `&&`/`||` + FIXPOINT v4 CONFIRMADO

### Regresión del fixpoint diagnosticada (causa raíz)
- `compiler_v4_self.nvc` (94,783 B, generado en corrida anterior) crasheaba: "Error: Índice N fuera de rango (largo: N)" dentro de `ejecutar_pipeline` (al self-compilar: Índice 117; al compilar match.nv: Índice 96).
- Comparación de disasm `self_disasm.txt` vs `v4_disasm.txt` probó la diferencia del bucle `mientras i < n && cs[i] != "\n" && cs[i] != "\r"`:
  - RUST-built (v4.nvc): `Load ti; Load tn; Lt; Store tmp; Load tmp; JmpIf → salta ANTES del ArrayGet` — CORTOCIRCUITO real.
  - LÚMEN-built (self.nvc): `Lt; ArrayGet; Neq; And; ArrayGet; Neq; And` — eager, lee `cs[ti]` con ti==n → out-of-bounds SOLO en el segundo-compilado (el primer compilado lo produciría igual, pero el bug solo explota dentro de la ejecución del self).
- Fix: helper `_cg_and_or(cg, izq, der, es_and)` en codegen.nv — cortocircuito real con `JmpIf`/`Jmp` + `PushBool` (&&: falsy→false en izq o der; ambos→true · ||: izq truthy→true; si no der decide). Binary despacha `&&`/`||`; el resto eager.

### Verificación
- FIXPOINT: compiler_v4.nv (99,993 B) → compiler_v4.nvc → self (112,368 B, exit 0, Instrs 12220) → self→self2 (112,368 B, exit 0) → byte-IDENTICAL (0 diffs). El self-compilado además compila match.nv sin crash → fuego_out ejecuta `dos`.
- fuego.ps1 completo: 389/389 compilan, 61 CORRECTOS (+1 vs 60 antes), 53 INCOMPATIBLES, 2 timeouts, 0 fallos — SIN regresión.
- Bug pre-existente en fuego.ps1:66: `.Trim()` sobre null en el detalle de INCOMPATIBLES (no afecta los contadores — el  ya estaba fijado).

### Archivos
- stdlib/compiler/codegen.nv (helper _cg_and_or + Binary despacha), stdlib/compiler/compiler_v4.nv (regenerado 99,993 B) + compiler_v4.nvc + compiler_v4_self.nvc/self2.nvc (112,368 B), stdlib/compiler/target.txt (match.nv).
- Scratch disasm v4_disasm.txt/self_disasm.txt pendientes de limpiar.

### Siguiente
- Sprint 7: VM en LÚMEN (vm.nv) + optimización velocidad (fixpoint ~10-18 min → objetivo <10s; demo ~15s).
- Sprint 8: dogfooding stdlib + docs + release v2.4.6.

---

## Sesión AI 21 Ago 2026 — PRODUCCIÓN REAL v3.0.0 (fixes escalables + bench 8 + headless + CHUNK_VERSION 7)

### Hechos
- **Fixes escalables llevados a producción (21 Ago):**
  - `crates/lumen-ir/src/builder.rs`: `last_significant()` ignora `Label/Nop/Phi` para `needs_return()`/`emit_return_if_needed()` en `Function`, `ImplRasgo`, `compile_lambda`, `build()` (`Halt`); `label_counter` global (no reseteo por función) evita colisión `Label(0)` en `codegen` global `label_map` que rompía `matematicas.nv` (`Variable 'n'`).
  - `crates/lumen-vm/src/vm.rs` + `crates/lumen-codegen/src/bytecode.rs`: `CHUNK_VERSION 7` con `FuncMeta.defaults: Vec<Option<DefaultValue>>` (`Int/Float/Str/Bool`) — `ir::Func.defaults` serializado; `VM bind_args` unificado para `Call`/`CallValue`/`run_function` (hilos) con `args.get(i).cloned().unwrap_or(Void)` + `DefaultValue` cuando `i>=args.len()`. `decode` acepta 6 y 7 (compat con `.nvc` antiguos).
  - `stdlib/graficos.nv:es_headless()` centralizado: `getenv("CI")`/`getenv("LUMEN_HEADLESS")` vía `__ffi` (`msvcrt`/`libc`/`libSystem`), `peek!=0` → `iniciar()`/`ventana()` retornan `false/0` sin `SDL_Init`. Demos con `si !iniciar() { retornar; }` ya son suficientes; guard per-demo redundante.
- **Bench formal 8** (`crates/lumen-bench/benches/benchmarks.rs`): `lexer_tokenize`, `parser_parse`, `pipeline_full`, `vm_fib_20` + 4 prod `prod_fallthrough_early_return`, `prod_defaults_callvalue`, `prod_matematicas_potencia`, `prod_graficos_headless` (`cargo bench -p lumen-bench`, `--quick` en CI, reporte `target/criterion/report/index.html`).
- **Tests producción:** 616 e2e (4 regresión: `test_regression_fallthrough_early_return`, `test_regression_matematicas_potencia`, `test_regression_defaults_callvalue`, `test_regression_lambda`) + 9 production (`crates/lumen-vm/tests/production.rs`: 3 aceptación + 2 performance + 4 integración) = **673 vm tests**, **917 workspace** (`cargo test --workspace` 0 FAILED). Ver `docs/produccion.md` §2.1.
- **CI `headless-check` nuevo** (`.github/workflows/ci.yml`): job Linux `env: LUMEN_HEADLESS=1 CI=1` corre `cargo test --workspace`, `cargo run --bin lumen -- check examples`, `cargo test --test production -- --nocapture`, `cargo bench -p lumen-bench -- --quick`. Validación sin display vía `es_headless()`.
- **Barrido:** `cargo run --bin lumen -- check examples` 389/389 (con `CI=1` 389/389), `LUMEN_HEADLESS=1 lumen run examples/graficos_*` → `init_fail_ok` sin `Variable 'a'`.
- **Versionado:** `Cargo.toml`/`VERSION` `3.0.0` · `CHUNK_VERSION 7` (fallback v6) · `CHANGELOG` v3.0.0 producción (21 Ago) · docs actualizadas (`README`, `AGENTS`, `roadmap`, `HERRAMIENTAS`, `MARKETING`, `siguiente`, etc.).
- **Verificado local:** `cargo test --workspace` 917, `cargo bench -p lumen-bench -- --quick` 8 OK, `LUMEN_HEADLESS=1 CI=1` con `es_headless()` (`Headless/CI detectado — demo omitida`).

### Próximos (post-producción v3.1, ver `docs/produccion.md` §6)
- `FuncMeta` defaults no literales (`b = foo()`) aún `None` → `Void`; evaluar thunk o `Expr` serializado.
- `label_map` per-function en `codegen` (hoy `label_counter` global mitiga, pero `codegen` global sigue frágil).
- `lumen fmt`/`lumen check` en pre-commit y `cargo bench` regresión >10% gate.

Archivos modificados (sesión producción): `crates/lumen-ir/src/builder.rs`, `crates/lumen-vm/src/vm.rs`, `crates/lumen-codegen/src/bytecode.rs`, `stdlib/graficos.nv`, `crates/lumen-bench/benches/benchmarks.rs`, `crates/lumen-vm/tests/e2e.rs` (4 regresión + `test_integracion_matematicas`), `crates/lumen-vm/tests/production.rs` (9), `.github/workflows/ci.yml` (job headless-check), `docs/produccion.md` (nuevo), `README.md`, `CHANGELOG.md`, `VERSION`, + docs sincronizadas (AGENTS/roadmap/etc.).
