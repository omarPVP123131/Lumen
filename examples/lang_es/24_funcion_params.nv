importar ingles;

funcion entero potencia(entero base, entero exp) {
    entero res = 1;
    entero i = 0;
    mientras (i < exp) {
        res = res * base;
        i = i + 1;
    }
    retornar res;
}

imprimir(potencia(2, 3));
imprimir(potencia(5, 2));
imprimir(potencia(3, 4));
