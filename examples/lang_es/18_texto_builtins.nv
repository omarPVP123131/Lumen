importar ingles;

texto s = "  LumenRust  ";

imprimir(__str_mayusculas(s));

texto t = "HOLA";
imprimir(__str_minusculas(t));

imprimir(__str_contiene(s, "Lumen"));
imprimir(__str_contiene(s, "Python"));

texto csv = "uno,dos,tres";
lista<texto> partes = __str_dividir(csv, ",");
para p en partes {
    imprimir(p);
}
