funcion texto repetir_t(texto s, entero n) {
    texto res = "";
    entero i = 0;
    mientras i < n {
        res = res + s;
        i = i + 1;
    }
    retornar res;
}

funcion texto ventana(texto titulo, texto contenido) {
    retornar repetir_t("─", 5);
}
