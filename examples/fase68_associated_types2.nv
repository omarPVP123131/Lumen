// Fase 68 — Associated Types: múltiples tipos asociados
rasgo Par {
    tipo A;
    tipo B;
    funcion A obtener_a(este);
    funcion B obtener_b(este);
}

funcion entero principal() {
    imprimir("rasgo con tipos asociados 'A' y 'B'");
    retornar 0;
}
