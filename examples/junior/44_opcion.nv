importar ingles;

function option<integer> encontrar(integer n) {
    if (n > 0) {
        return some(n * 2);
    }
    return none;
}

option<integer> a = some(42);
option<integer> b = none;
print(a);
print(b);

option<integer> r = encontrar(5);
print(r);
