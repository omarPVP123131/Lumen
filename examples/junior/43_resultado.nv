importar ingles;

function result<integer, string> dividir(integer a, integer b) {
    if (b == 0) {
        return error("division por cero");
    }
    return exito(a / b);
}

result<integer, string> r1 = dividir(10, 2);
result<integer, string> r2 = dividir(10, 0);
print(r1);
print(r2);
