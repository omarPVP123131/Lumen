importar ingles;

funcion T primero<T>(T a, T b) {
    retornar a;
}

funcion T ultimo<T>(T a, T b) {
    retornar b;
}

entero p = primero<entero>(5, 10);
entero u = ultimo<entero>(5, 10);
texto s = primero<texto>("hola", "mundo");

imprimir(p);
imprimir(u);
imprimir(s);
