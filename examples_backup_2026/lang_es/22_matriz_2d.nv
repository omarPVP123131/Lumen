importar ingles;

lista<lista<entero>> matriz = [[1, 2, 3], [4, 5, 6], [7, 8, 9]];

imprimir(matriz[0][0]);
imprimir(matriz[1][1]);
imprimir(matriz[2][2]);

entero suma = 0;
para fila en matriz {
    para celda en fila {
        suma = suma + celda;
    }
}
imprimir(suma);
