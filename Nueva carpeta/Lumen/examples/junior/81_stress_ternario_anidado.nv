importar ingles;

integer a = 10;
integer b = 5;
integer c = 3;
string r = a > b ? (b > c ? "a > b > c" : "a > b <= c") : "a <= b";
print(r);

integer x = -5;
string s = x > 0 ? "positivo" : (x < 0 ? "negativo" : "cero");
print(s);

print(10 > 5 ? (3 < 2 ? "X" : "Y") : "Z");
