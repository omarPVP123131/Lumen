// Fase 70 — Impl Trait return: funciones que devuelven un tipo rasgo
rasgo Mostrable {
    funcion texto mostrar(este);
}

funcion Mostrable fabricar() {
    retornar "hola";
}

funcion entero principal() {
    imprimir("retorno de rasgo");
    retornar 0;
}
